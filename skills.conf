# Claude Code skills repos installed on every VM by scripts/lib/install-claude-skills.sh
# Format: REPO|NAME   (REPO as in repos.conf: {org}/name, owner/name or a full git URL)
# Each skills/<skill>/ folder is symlinked to ~/.claude/skills/<skill>.
# Personal skills repos go in ~/.config/arrive-aml/skills.conf (same format).

{org}/azureml-skills|azureml-skills
