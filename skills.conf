# Claude Code skills repositories installed on every VM by scripts/lib/install-claude-skills.sh
# Format: REPO_URL|NAME
# Each repo's skills/<skill>/SKILL.md is symlinked into ~/.claude/skills/<skill>

git@github.com:rarko-arrive/azureml-skills.git|azureml-skills
